<section class="mb-25 lg:mb-35 padding-x">
	{% paginate collection.products by 2 %}
	  <h2 class="pt-10 pb-9 uppercase">{{ collection.title }}</h2>
		{% render 'collection-grid', show_image: true %}
		{% render 'pagination' %}
	{% endpaginate %}
</section>